{
  "name": "@document-tree/rdt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical Rich Document Tree model for DOCX and PPTX",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "prompts"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --write src test scripts manual/*.mjs README.md package.json tsconfig.json",
    "format:check": "prettier --check src test scripts manual/*.mjs README.md package.json tsconfig.json",
    "test": "node --test --import tsx test/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.9.6",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "fflate": "^0.8.3",
    "pdfjs-dist": "^6.2.108"
  }
}
